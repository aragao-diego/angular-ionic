(function(){
    'use strict';

    angular
        .module('usuario-logado.routes')
        .config(config);

    /* @ngInject */
    function config($ocLazyLoadProvider, $stateProvider, $urlRouterProvider){
        var STATE_PATH = '';
        var MODULE_PATH = 'modules/usuario-logado/';

    }
})();
