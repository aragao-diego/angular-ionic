(function(){
    'use strict';

    angular
        .module('menu.routes')
        .config(config);

    /* @ngInject */
    function config($ocLazyLoadProvider, $stateProvider, $urlRouterProvider){
        var MODULE_PATH = 'modules/menu/';
    }
})();
